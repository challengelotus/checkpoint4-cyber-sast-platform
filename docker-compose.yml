version: '3.8'

services:
  sast-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Mapeia o diretório local para dentro do container, permitindo Hot Reload
      - .:/app
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
